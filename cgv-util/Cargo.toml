[package]
name = "cgv-util"
version.workspace = true
edition.workspace = true

[dependencies]
ordered-float = "*"
anyhow = "*"
normalize-path = "*"
serde = {version="*", optional=true}
uuid = {version="*"}
nalgebra-glm = "*"

[features]
default = []
serde = ["dep:serde", "uuid/serde"]
